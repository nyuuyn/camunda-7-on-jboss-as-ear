<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>camunda-7-on-jboss-as-ear</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>camunda-7-on-jboss-as-ear</name>
  <description>
    Demonstrates deploying Camunda 7 as a genuinely container-integrated
    Java EE EAR on JBoss EAP, with no Camunda subsystem installed. See
    docs/arc42/README.md for the full architecture and rationale.
  </description>

  <modules>
    <module>camunda-engine</module>
    <module>process-application</module>
    <module>camunda-web-ui</module>
    <module>ear</module>
    <module>integration-test</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>

    <!-- Last Camunda 7 line on the javax.* namespace - see arc42 §2.1. -->
    <camunda.version>7.23.0</camunda.version>
    <javaee-api.version>7.0</javaee-api.version>
    <!-- Matches the jackson-databind version camunda-engine-rest-core resolves to transitively. -->
    <jackson-jaxrs-json-provider.version>2.14.1</jackson-jaxrs-json-provider.version>
    <!-- camunda-external-task-client's XML DataFormatProvider needs jakarta.xml.bind.* at
         runtime even on the javax.* artifact line; pinned to match camunda-parent:7.23.0. -->
    <jakarta.xml.bind-api.version>4.0.2</jakarta.xml.bind-api.version>
    <jaxb-impl.version>4.0.5</jaxb-impl.version>

    <maven-ejb-plugin.version>3.2.1</maven-ejb-plugin.version>
    <maven-war-plugin.version>3.4.0</maven-war-plugin.version>
    <maven-ear-plugin.version>3.3.0</maven-ear-plugin.version>
  </properties>

  <!--
    No scope is fixed here on purpose: the ear module needs these at
    "compile" scope (so they get bundled into ear/lib/), while camunda-engine
    and process-application only need them at "provided" scope (compiled
    against, supplied at runtime via ear/lib/). Each module declares its
    own scope explicitly.
  -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.camunda.bpm</groupId>
        <artifactId>camunda-engine</artifactId>
        <version>${camunda.version}</version>
      </dependency>
      <!-- Not camunda-engine-rest (classifier "classes") - that's nearly empty. -->
      <dependency>
        <groupId>org.camunda.bpm</groupId>
        <artifactId>camunda-engine-rest-core</artifactId>
        <version>${camunda.version}</version>
      </dependency>
      <!-- Cockpit/Tasklist/Admin, overlaid into camunda-web-ui.war - see ADR-9. -->
      <dependency>
        <groupId>org.camunda.bpm.webapp</groupId>
        <artifactId>camunda-webapp</artifactId>
        <version>${camunda.version}</version>
        <type>war</type>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.jaxrs</groupId>
        <artifactId>jackson-jaxrs-json-provider</artifactId>
        <version>${jackson-jaxrs-json-provider.version}</version>
      </dependency>
      <dependency>
        <groupId>org.camunda.bpm</groupId>
        <artifactId>camunda-external-task-client</artifactId>
        <version>${camunda.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.xml.bind</groupId>
        <artifactId>jakarta.xml.bind-api</artifactId>
        <version>${jakarta.xml.bind-api.version}</version>
      </dependency>
      <dependency>
        <groupId>com.sun.xml.bind</groupId>
        <artifactId>jaxb-impl</artifactId>
        <version>${jaxb-impl.version}</version>
      </dependency>

      <!-- Provided by JBoss EAP itself -->
      <dependency>
        <groupId>javax</groupId>
        <artifactId>javaee-api</artifactId>
        <version>${javaee-api.version}</version>
        <scope>provided</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-ejb-plugin</artifactId>
          <version>${maven-ejb-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <version>${maven-war-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-ear-plugin</artifactId>
          <version>${maven-ear-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
